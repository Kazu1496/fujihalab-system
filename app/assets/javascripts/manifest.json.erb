<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Fujihalab System",
  "short_name": "F..Lab System",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "favicon/icon-#{dim}.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#FFA000",
  "background_color": "#FFFFFF",
  "display": "standalone",
  "orientation": "portrait"
}
