- provide(:title, '出席状況')
%p#notice= notice

%h1 出席状況 in FujihaLab
%table
  %thead
    %tr
      %th 氏名
      %th 在室状況
      %th 総在室時間
      %th 詳細情報
  %tbody
    - @users.each do |user|
      %tr
        %td
          = user_image_tag(user, "")
          = name = user.nickname.present? ? user.nickname : user.name
        %td= user.status == 1 ? '入室中' : '退室中'
        %td #{user.total_time} 時間
        %td= link_to 'Show', user
